# Зависимости для запуска автотестов (pytest).
#
# В отличие от requirements/base.txt этот файл НЕ закрепляет жёстко версии
# PyYAML и zeep: PyYAML==6.0 и zeep==4.2.1 из base.txt не собираются под
# Python 3.13 (PyYAML — Cython build issue, zeep<4.3 импортирует удалённый
# модуль stdlib `cgi`). Прод-образ ставится через base.txt на Python 3.10,
# где эти пины собираются; локальный/CI прогон тестов может идти на
# Python 3.10–3.13 c этим файлом.
#
# Установка:
#   python -m pip install -r requirements/dev.txt
#
# Запуск:
#   pytest

# --- Runtime-deps (нужны для импорта src/main.py и пакета) ---
graphviz2drawio==1.1.0
pika==1.3.0
PyJWT==2.10.1
psycopg2-binary>=2.9.10,<3
requests==2.32.3
requests-toolbelt==1.0.0
Jinja2==3.1.6
PyYAML>=6.0.1,<7
uvicorn==0.35.0
fastapi==0.133.0
fastjsonschema==2.21.1
pydantic==2.12.5
pydantic-settings==2.10.1
pydantic_core==2.41.5
zeep>=4.3.1,<5
pycamunda==0.6.1
prometheus_client==0.21.0
python-dotenv>=1.0
# legacy-cgi требуется для zeep на Python>=3.13 (модуль cgi удалён из stdlib).
legacy-cgi; python_version >= "3.13"

# --- Test runner ---
pytest==8.4.2
pytest-cov==6.0.0
httpx==0.28.1
